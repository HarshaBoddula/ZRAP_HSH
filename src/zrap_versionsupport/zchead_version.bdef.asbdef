projection;
strict ( 2 );

define behavior for ZCHEAD_VERSION //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use association _Details { create; }
}

define behavior for ZClineitem_VERSION //alias <alias_name>
{
  use update;
  use delete;

  use association _Header;
}